<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <title>Filmek</title>
    <link rel="stylesheet" href="/style/style.css">
    <link rel="stylesheet" href="/style/update.css">
    <script src="https://cdn.jsdelivr.net/npm/axios@1.1.2/dist/axios.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
</head>
<body>
<h1>Update Award</h1>
<nav></nav>


<main>
    <form>
        <label>
            <span>Name:</span>
            <input class="name" type="text" value="<%= award.nev %>" />
        </label>
        <label>
            <span>Description:</span>
            <input class="description" type="text" value="<%= award.leiras %>" />
        </label>

        <div class="button-container">
            <button onclick="update(<%= award.id %>)">✔</button>
            <button><a href="/awards">x</a></button>
        </div>
    </form>

    <script>
        function update(id) {
            let award = {
                id,
                nev: $('.name').val(),
                leiras: $('.description').val(),
            }

            axios.post(`http://localhost:3001/award-update`, {award})
                .then(() => {
                    alert('Award update ok!')
                }).catch((response) => {
                console.log(response)
            });
        }
    </script>
    <!--    </form>-->
</main>

</body>
</html>